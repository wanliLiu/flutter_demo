import 'package:flutter/material.dart';

class TabSchool extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const Align(
      child: Text(
        "School",
        textScaleFactor: 5,
      ),
    );
  }
}
