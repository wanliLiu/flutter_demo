import 'package:flutter/material.dart';

class TabPicture extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Align(
      child: Text(
        "Scrol",
        textScaleFactor: 5,
      ),
    );
  }
}
